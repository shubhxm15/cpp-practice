#include<iostream>
using namespace std;
int main(){
    float x = 123.1234;

    cout<<"Integer value of x is: "<<int(x);
                 // OR                                 // Basically its a way to change point(.) value into integer type
    cout<<"\nInteger value of x is: "<<(int)x<<endl;

        return 0;
}
